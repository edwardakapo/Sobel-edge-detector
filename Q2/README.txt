101095403 
Oluwademilade Edward Akapo
4102 a1 q2

To run the code make sure you have all dependecies installed. Opencv, Mathplotlib and Numpy
Then scroll to the end of the code to see the function calls, there you can change the input image, sigma and threshold. After running the outputs will be printed to screen and saved to a folder.